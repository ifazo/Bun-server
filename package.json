{
  "name": "bun-server",
  "module": "index.ts",
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun build index.ts",
    "watch": "bun --watch run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "type": "module",
  "dependencies": {
    "mongodb": "^6.10.0"
  }
}